<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vehicleservicemanagementapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Helpers.PDFUploadActivity"></activity>
        <activity android:name=".Helpers.NightMode" />
        <activity android:name=".Activites.VehicleDetailActivity" />
        <activity
            android:name=".Activites.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activites.LoginActivity" />
        <activity android:name=".Activites.HomeActivity" />
        <activity android:name=".Activites.RegisterActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".About" />
        <activity android:name=".VehicleInventory" />
        <activity android:name=".Main_Menu" />
        <activity android:name=".Login_form" />
        <activity android:name=".Signup_Form" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>